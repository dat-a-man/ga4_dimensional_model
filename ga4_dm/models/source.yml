version: 2

# The source lets the user define the data currently stored in dwh.
# "- name" is what dbt uses to reference the source in the models.
# "schema" is interchangeable with the BigQuery concept of dataset.
# "database" is interchangeable with the BigQuery concept of project.
#  https://docs.getdbt.com/docs/build/sources

sources:
  - name: amn_ga4_select_star
    database: dlthub-analytics
    schema: amn_ga4_select_star
    tables:
      - name: events_20231009
      - name: events_20231009__event_params

      

  
    